export interface CDJState {
    playing: boolean;
    sync: boolean;
    master: boolean;
    onAir: boolean;
}
export declare const mapCdjState: (flag: number) => CDJState;
export interface CDJStatus {
    name: string;
    device: number;
    state: CDJState;
    speed: {
        pitch: number;
        bpm: number;
        current: number;
    };
}
export declare const test: (buffer: Buffer) => boolean;
export declare const parse: (buffer: Buffer) => CDJStatus;
