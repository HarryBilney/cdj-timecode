import { CDJState } from './cdj-status';
export interface MixerStatus {
    name: string;
    device: number;
    state: CDJState;
    speed: {
        pitch: number;
        bpm: number;
    };
}
export declare const test: (buffer: Buffer) => boolean;
export declare const parse: (buffer: Buffer) => MixerStatus;
