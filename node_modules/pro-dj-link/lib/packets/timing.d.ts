export interface TimingPacket {
    speed: {
        original: number;
        pitch: number;
        current: number;
    };
    device: number;
    name: string;
}
export declare const test: (buffer: Buffer) => boolean;
export declare const parse: (buffer: Buffer) => TimingPacket;
