import { CDJState } from './packets/cdj-status';
import { Observable } from 'rxjs';
export interface DeviceStatus {
    name: string;
    device: number;
    state: CDJState;
    speed: {
        pitch: number;
        bpm: number;
        current: number;
    };
}
export declare class VirtualCdj {
    private device;
    private broadcast;
    private mac;
    private ip;
    private socket;
    private _bpm;
    private _devices;
    constructor(networkDevice: string, device?: number);
    private keepalive;
    readonly devices: Observable<DeviceStatus[]>;
    readonly bpm: Observable<number>;
}
