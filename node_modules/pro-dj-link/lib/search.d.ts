import { Observable } from 'rxjs';
export interface BaseDevice {
    name: string;
    device: number;
}
export declare const search: () => void;
export declare const devices: Observable<BaseDevice[]>;
